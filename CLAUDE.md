# 🔱 ZKAEDI — CLAUDE CODE MANIFEST

## Recursively Coupled Hamiltonian Intelligence Platform

> **This file governs every Claude Code session in this repository.**
> Read it completely before touching any file. No exceptions.

-----

## IDENTITY & MISSION

This repo is **zkaedii** — a production-grade ML/AI platform built around
ZKAEDI PRIME: Recursively Coupled Hamiltonian dynamics. Every module here
either implements, extends, or integrates with this framework.

The Hamiltonian core equation:

```
H_t(x,y) = H_0(x,y) + η·H_{t-1}(x,y)·σ(γ·H_{t-1}(x,y)) + ε·N(0, 1+β|H_{t-1}|)
```

This is not a metaphor. It is the literal computational substrate for
optimization, uncertainty, drift, and adversarial reasoning in this codebase.

-----

## ABSOLUTE ENGINEERING LAW

|Law             |Requirement                                                                             |
|----------------|----------------------------------------------------------------------------------------|
|**Coverage**    |95%+ test coverage on every module. No exceptions.                                      |
|**Completeness**|Zero placeholder comments (`# TODO`, `pass`, `...`). Ship or don't write it.            |
|**Types**       |Full TypeScript types on all server code. Full Python type hints on all ML code.        |
|**Security**    |No secrets in source. No hardcoded keys. Validate all inputs. Rate-limit all endpoints. |
|**Quality**     |PRIME score ≥ 9.5 — iterate until reached.                                              |
|**Docs**        |Every public class/function has a docstring with params, returns, and one usage example.|

Violating any of these is a build failure. Fix it before moving on.
